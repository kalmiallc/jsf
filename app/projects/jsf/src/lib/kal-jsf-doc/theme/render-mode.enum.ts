export enum JsfThemeRenderMode {
  /**
   * Renders the JSF builder form.
   */
  Form,

  /**
   * Renders a router outlet.
   */
  RouterOutlet,

  /**
   * Only render the styles.
   */
  Styles
}
